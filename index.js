const SelectionInfo = require('./src/selectioninfo');
const TextLine = require('./src/textline');
const TextLineReader = require('./src/textlinereader');

module.exports = {
    SelectionInfo: SelectionInfo,
    TextLine: TextLine,
    TextLineReader: TextLineReader
};
